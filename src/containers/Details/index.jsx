import React from 'react';
import PropTypes from 'prop-types';

// import { Container, Header, Footer
//  } from '../Layout/components';

const Details = ({ informationUrl }) => {
  return (
    <div>details</div>
  );
}

Details.propTypes = {
    informationUrl: PropTypes.string.isRequired
  };
export default Details;